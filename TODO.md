# CMAP website follow-up

- Revalidate all curated experimental tables against the manuscript after any numerical revision.
- Add the introduction video to `assets/videos/` and register it in `data/media.json`.
- Add real-robot and success/failure videos only when approved media is available.
- Keep `REVIEW_MODE=true` through anonymous review.
- After acceptance, add verified authors, affiliations, code, video, arXiv, publication metadata, and BibTeX in `config/site.json`.
- Replace or update the copied manuscript PDF after every paper recompilation by rerunning the synchronization command.
- Review the paper-sensitive terminology and asset set before each public deployment.
